## What

<!-- What does this change, in one or two sentences? -->

## Why

<!-- Motivation and context. Link issues. Note any architecture impact
(see docs/architecture/OVERVIEW.md) and the plan doc in docs/plans/ if one
exists. -->

## Validation

- [ ] `uv run pytest tests/` passes
- [ ] `uv run ruff check .` and `uv run ruff format --check .` pass
- [ ] `uv run mypy loops` passes
- [ ] Conformance suite updated if protocol-visible behavior changed
- [ ] Docs updated (`docs/architecture/`, `docs/specs/`, README) if applicable
- [ ] CHANGELOG.md updated under [Unreleased]
