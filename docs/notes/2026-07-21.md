# 2026-07-21 Notes

## TUI → standalone harness host

Executed `docs/plans/2026-07-21-tui-standalone-harness.md` (user decision:
not all-in on MCP; make the TUI the standalone harness first).

### What shipped (P0 + P1)

- **Ctrl+C → task.interrupt**: the REPL catches KeyboardInterrupt during
  live waits and raises a human interrupt checkpoint (task → blocked,
  resolvable) instead of killing the session. `_interrupt_active` helper,
  offline-tested.
- **`/tasks` + `/use <id>`**: task list with active marker; switch active
  task (terminal tasks rejected, unknown ids NOT_FOUND-rendered).
- **`/handoff <agent>`**: `ownership.transfer` through the controller;
  harness fork fires automatically on pi/claude — the receiving agent
  inherits full session context.
- **`/artifacts` + `/show <id>`**: delivery visibility before review.
- **Startup recovery**: `--resume <session_id>` jumps into a saved session;
  the banner always shows adapter/principal/session/open-inbox count and
  prints the open inbox.
- **Grouped `/help`** (Session / Tasks & artifacts / Human-loop decisions /
  Continuous control / Other) and **`--model`** passthrough
  (codex `-m`, pi/claude `--model`, kimi `-m`).
- Continuity flag stripping relaxed: `--no-session` /
  `--no-session-persistence` are now stripped with continuity on for ANY
  command (not just defaults), so custom commands (e.g. with `--model`)
  keep session resume working.

### Live smoke (this machine, `--adapter pi`)

piped REPL: prompt(记住暗号 XJ42-QZ-2026) → `/tasks` → `/handoff
agent_pi_two` → 复述暗号. All worked: task created, /tasks rendered,
handoff returned the forked run, and the steered follow-up in the forked
session recited the codeword — the full harness story (continuity + fork)
through the TUI. Claude path identical to the earlier fork probe; not
re-smoked to save calls.

### Verification

313 passed / 3 skipped (13 new TUI tests: commands, Ctrl+C semantics,
model passthrough); mypy strict clean (53 files); ruff clean.

## Runtime adapter / model switching in the TUI

User request: switch adapter and model without restarting.

- `build_client(..., store=)` — clients can be rebuilt over the SHARED
  HumanLoopStore, so task history survives a switch.
- `TUIController(adapter_builder=...)` — host injects a factory;
  `/adapter <name>` rebuilds the client and, when a task is active,
  `ownership.transfer` hands it to the new adapter (fresh run, fork if the
  target CLI supports it). `/model <name>` rebuilds with the new model on
  live adapters (records only on fake). `set_preference` gained `adapter`.
- 3 offline tests (rebuild + handoff + shared store identity, validation,
  model rebuild vs fake).

### Live smoke (pi → kimi)

pi delegate memorized the codeword → `/adapter kimi` →
`adapter=kimi; active task handed off (run run_xj42_20260721_001)` →
follow-up steer on kimi. The switch, handoff, and continuation all worked;
kimi answered WITHOUT the codeword — the documented fork-matrix boundary
(kimi has no native fork, so no context inheritance), not a bug. A
pi→claude switch would inherit context via `--fork-session`.

## TUI /adapter 深度修复（用户直播抓出的三个问题）

1. **进度标签停留在启动 adapter**：REPL 每轮从 `sessions.resume(...).adapter`
   取标签（原来用 `args.adapter` 固定值）。
2. **跨 CLI 切换的 handoff 是静默 no-op（根因）**：`_switch_adapter` 把任务交给
   `self.agent_id`（"agent_tui"），而 TUI 任务的 assignee 本来就是
   "agent_tui"——`ownership_transfer` 的 `to != assignee` 条件不满足，adapter
   handoff 从未触发：没有新 run、没有会话绑定，后续 steer 全靠
   `_require_run` 的宽容分支一次性调用，上下文与 fork 全丢（pi 会话文件证实
   每次调用都是新会话）。修复：交给 `agent_{target}`（assignee 必不同，真触发），
   并给 `/handoff` 加"已分配给该 agent"的防呆。
3. **跨 CLI 上下文简报**：无共享会话时（跨 CLI），切换后经现有 steer 通道把
   最近 transcript 简报注入新会话（HOST NOTE 强指令格式），结果消息明示
   "no shared session history"。同 CLI（如仅换 model）走原生会话 fork，不注入。

### 直播复测（用户原场景，kimi → pi）

handoff 真触发（"Ownership transferred from agent_tui to agent_pi"）、
briefing 落进恢复会话、pi 正确回答出暗号 `123bcd`；进度标签随会话切换
（kimi → pi）。

### 验证

318 passed / 3 skipped（新增跨 CLI 简报与同 CLI 不简报两组测试）；
mypy strict clean；ruff clean。

## Fake adapter 降级 + RunRegistryAdapter 生产基类

用户建议的落地版本（不动 API 语义）：

- 新 `loops/hlp/adapters/_registry.py::RunRegistryAdapter`：run 注册机制
  （`_runs`/calls/`_require_run`/`_next_run_id` + block/resume/steer/cancel
  四个共享记录实现）。`ProcessAgentAdapter` 改继承它——生产链不再继承
  "Fake"。7 处显式 `FakeAgentAdapter.*` 调用同步换名。
- `FakeAgentAdapter(RunRegistryAdapter)` 只剩合成 run 的 delegate/handoff
  与 fake healthcheck（测试替身本色）；`FakeHarnessAdapter`/`InMemory`
  别名不变。
- 顶层 `loops` 的 `__all__` 移除 `FakeAgentAdapter`/`FakeHarnessAdapter`/
  `InMemoryAgentAdapter`（无人从顶层导入，零破坏）；`loops.hlp` 与
  `loops.hlp.adapters.fake` 照常可用；`HLPClient` 默认 adapter 与 TUI
  `--adapter fake` 离线模式保留。

318 passed / 3 skipped，mypy strict（54 文件），ruff 全清。

## Harness 聚合状态投影：RunProgress + TUI /progress

Executed `docs/plans/2026-07-21-harness-progress-projection.md`. 边界判断：
todo/sub-agent 状态是 harness planning 内部（附录 C §C.2 高频可丢语义），
只做 ephemeral 投影，不进 spec/audit/责任面。

### 实现

- `objects.py`：`ProgressItem` / `SubAgentStatus` / `RunProgressSnapshot`
  （frozen 值对象，最新态快照）。
- `_parsing.py::progress_from_events`：codex `todo_list` item 与 claude
  `TodoWrite`（status 映射）/`Task` tool_use（parent_tool_use_id 链接）
  + tool_result 收 agent 完成态。
- `HarnessAdapterBase`：六个操作解析后累积 `_run_progress`；合并语义：
  todo 重写即替换、无 in_progress 时提升首个 pending（修掉了"已有
  in_progress 还提升"的双提升 bug）；`run_progress(run_id)` 可选能力，
  pi/kimi wire 无来源返回 None。
- `HLPClient.run_progress` 门面；TUI `/progress`（✓/◐/○ 清单 + agent
  树）+ prompt 后自动紧凑摘要行（`progress 2/4 todos · 1 agents running`）。

### 验证

- 离线 6 个测试（codex/claude 快照、跨操作累积、pi/kimi fallback、
  facade None、TUI 渲染集成）；324 passed / 3 skipped。
- 直播（本机 codex 真实 todo 探测）：snapshot 正确返回
  1 in_progress + 2 pending 与 summary。

### 非目标（文档已注明）

progress 不作责任依据、不进 spec/audit/wire schema；pi/kimi todo 待
wire 长出再补；不做进度历史回放（叙事仍走 audit）。
