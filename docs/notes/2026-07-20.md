# 2026-07-20 Notes

## Projection hardening + reference-profile hardening

Executed `docs/plans/2026-07-20-projection-profile-hardening.md`: make the
four-CLI projection and the HLP-industrial reference profile solid, per the
adoption-path decision (harden these first; true session continuity later).

### W1 — native structured output for the projection contract

The weakest link was prompt-disciplined JSON echoes (live failures: claude
`status:"acknowledged"`, pi missing correlation echo). Codex and Claude Code
both have native structured-output switches, now wired in protocol mode:

- `HLP_RESULT_SCHEMA` (`_parsing.py`): closed envelope `run_id`,
  `correlation_id`, `status(ok|error)`, `summary`, `error`.
  Live iteration with codex strict mode taught: `additionalProperties: false`
  required at every object level, and `required` must list every property —
  so no optional `hlp` field; human-loop events keep flowing through
  intermediate stream text (existing extraction handles it).
- `ClaudeCodeHarnessAdapter` / `ClaudeCodeCLIAdapter`: `--json-schema`
  (inline). `CodexHarnessAdapter` / `CodexCLIAdapter`: `--output-schema`
  (schema written once per process to a system temp file, `atexit` cleanup).
- Chat mode never gets the flag (free-form replies); custom commands are
  never rewritten; Kimi/Pi have no such flag (docstring-noted).

Live verification (this machine, both via `HLPClient.delegate`):

- claude: result envelope carries `structured_output` with the exact
  correlation echo; **parse fix**: extraction now digs `structured_output`
  (it was missing from the dig keys — would have silently dropped the
  validated envelope).
- codex: `item.completed` agent_message text is exactly the schema object;
  correlation + status deterministic.

New `tests/test_hlp_projection_robustness.py` (14 tests): schema wiring per
adapter/mode/custom-command, closed-schema invariant, and wire variance per
CLI (markdown fences, prose, CRLF, unicode, duplicate embedded hlp event →
projects once, missing correlation tolerated, malformed line fail-fast,
`structured_output` preference, codex `item.completed` shape).

### W2 — reference profile recovery + timeout semantics

- `HumanLoopOperations.pending_adapter_outbox()` (+ `HLPClient` facade):
  crash-pending intents are queryable; recovery = retry the original
  idempotency-keyed op (reuses persisted intent, no duplicate side effects).
  Conformance test drives the full loop (crash → pending → retry →
  succeeded).
- `HumanLoopOperations.expire_due_checkpoints(now)` (+ facade): §7.2 sweep —
  expires due pending checkpoints through the audited `checkpoint_expire`;
  task stays blocked (pure suspension); repeated sweeps are no-ops.

### Verification

- 262 passed / 2 skipped (14 robustness + 2 W2 new); mypy strict clean
  (44 files); ruff lint+format clean.
- Live probes: claude + codex protocol delegate both return the exact
  envelope via native schema enforcement.

### Follow-ups

- Session-resume continuity (`kimi -r` / `claude -r` / `pi --resume`) —
  the remaining big adapter item.
- Dependabot PRs opened by the new config are pending review.

## Session-resume continuity (four CLIs, live-verified)

Executed `docs/plans/2026-07-20-session-resume-continuity.md`: follow-up ops
resume the same native CLI session instead of fresh one-shot processes.

### Mechanism (all live-probed)

- Session ids from the wire: codex `thread.started.thread_id`, claude
  per-event `session_id`, kimi `role=meta` `session_id`, pi
  `type=session.id` — one extractor `session_id_from_events`.
- Resume commands per CLI: `codex exec resume [opts] <id>` (options FIRST —
  a wrong-order draft failed live), `claude --resume <id>`,
  `kimi --session <id>`, `pi --session <id>`.
- Persistence blockers found live and auto-stripped when continuity is on:
  codex `--ephemeral` ("no rollout found for thread id" without this fix),
  pi `--no-session`, claude `--no-session-persistence`.
- Codex resume keeps `--json` + `--output-schema` (verified compatible).
- `session_continuity=False` restores exact one-shot behavior (incl. flags).

### Live proof (this machine)

Codeword probe (`examples/hlp_session_continuity_e2e.py`, registered as
`loops-hlp-continuity-e2e`): delegate "记住暗号 XJ42-QZ-2026" → block →
resume → steer "复述暗号". All four CLIs: 3/3 follow-up ops used the resume
command and the model recited the codeword from session context:

- codex: session `019f7eef-…` ✓
- claude: session `b06cb8ef-…` ✓
- kimi: `session_839379b1-…` ✓
- pi: session `019f7ef3-…` ✓

New `tests/external/test_hlp_session_continuity_real_cli.py` gates the same
probe behind `HLP_RUN_EXTERNAL_CLI_E2E=1`; offline contract coverage in
`tests/test_hlp_session_continuity.py` (binding, resume commands, fallback,
continuity-off, schema/persistence invariants).

Continuity semantics documented as "a new turn in the same native session
context" — the strongest form these CLIs offer today, not frozen-process
resumption.

### Correlation injection for schema-less CLIs

Full-suite re-run after continuity surfaced the remaining flake: pi's
delegate omitted the correlation echo entirely (prompt-contract variance —
pi/kimi have no structured-output flag). Hardening: `CodexHarnessAdapter._execute`
now fills an ABSENT `correlation_id` from the request (local binding, same
philosophy as the `run_id` fallback); present-but-different echoes are still
rejected by validate-when-present. Offline tests pin both directions. Pi-only
lifecycle verified green with the fix; full external suite re-run recorded
above.

### Final record

`HLP_RUN_EXTERNAL_CLI_E2E=1 pytest tests/external/` → **3 passed**
(lifecycle for codex/kimi/claude/pi, soft-harness live, session-continuity
codeword probe for all four) in ~15 min. Offline: 268 passed / 3 skipped,
mypy strict clean (44 files), ruff clean.

## Session forking: true handoff semantics (pi/claude, live-verified)

Executed `docs/plans/2026-07-20-session-forking-handoff.md`. Handoff now
forks the native session where the CLI supports it:

- `_command_for_prompt` handoff branch → `_fork_command(session_id, prompt)`;
  base returns None (no fork) → one-shot fallback. pi:
  `pi --mode json --fork <src> -p`; claude:
  `claude --resume <src> --fork-session -p ...`; codex/kimi: None.
- New run binds the forked session id from the wire via existing `_bind_session`.

### Live proof — controlled experiment

First probe iteration was flawed: the codeword rode the handoff context, so
every CLI "passed" (envelope knowledge, not inheritance). Fixed design:
handoff context carries NO codeword; the receiving run can only know it via
session inheritance. Results (this machine):

- pi: fork → NEW session `019f7f62-41f1…` (≠ src), codeword recited ✓
- claude: fork → NEW session `81242f64-…` (≠ src), codeword recited ✓
- codex: one-shot (no native fork), codeword NOT found — honest fallback ✓
- kimi: one-shot, codeword NOT found ✓

`tests/external/test_hlp_session_continuity_real_cli.py` now asserts both
directions (fork inheritance for pi/claude, no inheritance for codex/kimi);
offline tests pin fork commands, new-session binding, and fallbacks.

Capability matrix is documented in README; when codex/kimi ship native fork
support, `_fork_command` is the single extension point to enable it.
