# 2026-06-19

## HACP 参考实现落地

- 在 `loops/loop2/` 下实现 HACP 0.1.0-draft 的最小可验证参考实现，对应 spec `docs/specs/HACP.md`。
- 新增运行时依赖 `python-ulid`（spec §3.1 要求 ULID + 前缀）和 `typing-extensions`（python-ulid 的传递依赖，uv.lock 未自动解析，显式补上）。
- 实现覆盖 spec §8 一致性级别全部 7 条硬指标：7 个一等对象、21 个操作、Task 状态机、不可变性、审计、前置条件、层间契约。

## loop2 包结构

- `loops/loop2/_ids.py` — ULID + 前缀生成器（task_/ckpt_/rev_/art_/led_/aud_）。
- `loops/loop2/types.py` — `ProtocolError`（含错误码）、`Literal` 类型别名（TaskState 等）。
- `loops/loop2/objects.py` — 7 个一等对象 dataclass。不可变值对象 frozen（TaskSpec/OwnershipTransfer/CheckpointResolution/ReviewComment/ArtifactPayload/ArtifactRef/LedgerEntry），状态对象可变（Task/Checkpoint/Ownership/Review/Artifact/Ledger）。
- `loops/loop2/state_machine.py` — 显式合法转移表 `LEGAL_TRANSITIONS` + `check_transition` 校验，非法转移抛 `PRECONDITION_FAILED`。
- `loops/loop2/audit.py` — `AuditEvent`（frozen）+ `AuditLog`（append-only，单调 seq，支持 query/replay）。
- `loops/loop2/store.py` — `HACPStore` 内存存储，按 id 索引各对象，artifacts 按 (id, version) 二元组建索引。
- `loops/loop2/contracts.py` — `AAPBridge` Protocol（HACP→AAP 层间接口）+ `InMemoryAAPBridge` stub（记录调用不执行，验证 TaskID 贯穿）。
- `loops/loop2/operations.py` — `HACPOperations` facade，21 个操作统一结构：前置条件→状态转移→audit→层间契约。
- `loops/loop2/__init__.py` — 公开 API re-export。

## 关键实现决策

- **风格对齐 loop0**：stdlib dataclasses + `typing.Literal`（非 enum）+ `from __future__ import annotations`，无 pydantic。
- **分层纪律**：loop2 刻意不 import loop1/loop0，证明协议层可独立存在（transport-agnostic），有 `test_loop2_does_not_import_lower_layers` 守护。
- **测试风格**：遵循 `tests/test_loops_core.py` 约定——同步 `def test_*` + `asyncio.run()` 驱动 async 操作，不引入 pytest-asyncio。
- **状态机修正**：`review_submit` 先 `review_ready→under_review` 再按 verdict 转，对应 spec §3.3 合法路径。
- **CLI 本阶段不做**：先验证协议语义，CLI 留后续。

## 测试

- `tests/test_loop2_hacp.py`：31 个测试，覆盖状态机、前置条件、不可变性、审计、层间契约、端到端闭环（spec 附录 A "Review PR #1234"）、ownership 流转、artifact 版本链、分层纪律。
- 全仓库 `uv run pytest -q`：62 passed（31 现有 loop0 + 31 新 HACP），零失败。

## 文档

- 新增 `docs/plans/2026-06-19-hacp-reference-impl.md`（本实施计划落盘）。
- 新增 `docs/architecture/loop2.md`（loop2 架构说明，补齐 AGENTS.md 要求的 `loop<i>.md` 文档）。
- `loops/__init__.py` docstring 更新：说明 loop2 作为独立子包入口，不 re-export 到顶层（保持 loop0 为默认 API）。

## Loops Protocol public website

- Reworked the VitePress site in `docs/site/` into a complete English public-facing protocol website.
- Added the public website plan at `docs/plans/2026-06-19-loops-protocol-public-website.md`.
- Replaced Chinese-first site copy with English pages for overview, implementation guide, conformance, inter-layer contracts, and the HACP/AAP/CAP protocol pages.
- Added visual assets for the public site: `docs/site/public/logo.svg` and `docs/site/assets/stack.svg`.
- Verified the site with the bundled Node runtime: VitePress build passes, rendered HTML pages contain no Chinese text, and browser checks pass on desktop and mobile viewports.
